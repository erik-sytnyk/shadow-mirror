> **Ребрендинг (2026):** Проект переименован в **Shadow Mirror** (shadow-mirror.dev). Концепция Юнговской Тени — то, что человек подавляет и отказывается признавать.

1. Системный промпт для контекста Windsurf
Добавь это в правила или в промпт для генерации:

"Реализуй логику Dark Mirror. Система должна вести диалог строго по протоколу: анализ входного текста -> 2-3 архетипических вопроса -> финальный бесстрастный разбор сути. Тон ответов: холодный, ясный, без юмора и морализаторства. Использовать модель GPT-4o через API." 
+2

2. Структура главной страницы (Page.tsx)
Зеркало должно выглядеть так, чтобы в него было страшно смотреть. 

TypeScript
// Для Windsurf: Создай минималистичный интерфейс
// Фон: #000 (черный), Текст: #E4E4E7 (zinc-200)
// Акцент: Свечение вокруг текстового поля #1E3A8A (dark blue) 

const DarkMirrorUI = () => {
  return (
    <div className="min-h-screen bg-black text-zinc-200 flex flex-col items-center justify-center p-4">
      <header className="mb-12 text-center">
        <h1 className="text-4xl font-light tracking-widest uppercase mb-2">Dark Mirror</h1>
        <p className="text-zinc-500 italic">"Ты не тот, кем себя считаешь." [cite: 40-41]</p>
      </header>
      
      {/* Окно диалога с эффектом глубины */}
      <div className="w-full max-w-2xl border border-zinc-800 bg-zinc-900/30 p-8 rounded-lg shadow-2xl shadow-blue-900/20">
        {/* Здесь логика чата */}
      </div>
      
      <footer className="mt-12 text-zinc-700 text-sm">
        Тот, кто видел суть. Имя не имеет значения. [cite: 45]
      </footer>
    </div>
  );
}
3. Первый тест системы
Когда Windsurf соберет билд, протестируй его на себе еще раз, но уже через интерфейс. Напиши ему: "Я хочу построить успех на Raive AI, чтобы забыть о боли Orbios". 

Если Зеркало ответит тебе чем-то вроде: "Ты ищешь не успеха, а анестезии", значит, продукт готов к тестам на других.